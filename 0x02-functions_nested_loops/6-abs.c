#include "main.h"

/**
 * _abs - compute absolute value of integer
 * @j: integer to the worked on
 * Return: Absolute integer value
 */

int _abs(int j)
{
	if (j >= 0)
		return (j);
	else
		return (-j);
}
